import { Banner, ClassSec, Events, Testimonial } from "../utils/helper";

const ClassesPg = () => {
	return (
		<main>
			<Banner title="CLASSES" />
			<ClassSec />
			<Testimonial />
			<Events />
		</main>
	);
};

export default ClassesPg;
